services:
  backend:
    build:
      context: ./fullstack-project/backend
    ports:
      - "8000:8000"
    volumes:
      - ./fullstack-project/backend:/app

  server:
    build:
      context: ./fullstack-project/frontend
    environment:
      NODE_ENV: production
    ports:
      - 5173:5173