

# TODO: make sure there is a router for deleting a cart, which would just be setting the cart to a new Cart() object (restores defaults)

# TODO: ensure each method calls require_role for Customer, to both ensure role is there and that they're logged in. Cast output as Customer